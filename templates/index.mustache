<!DOCTYPE html>
<html>
<head>
	<title>beta.aintaer.com</title>
	{{> head}}
</head>
<body>
	{{> navigation}}
	{{> header}}

	<aside class="quicknav">
		{{#prevPage}}<a href="{{url}}" class="left icon-arrow-left5"></a>{{/prevPage}}
		{{#nextPage}}<a href="{{url}}" class="right icon-arrow-right5"></a>{{/nextPage}}
	</aside>

	{{# articles}}<article>{{> index}}</article>{{/ articles}}
	{{> footer}}
</body>
</html>
