<!DOCTYPE html>
<html>
<head>
	<title>beta.aintaer.com</title>
	{{> head}}
</head>
<body>
	{{> navigation}}
	{{> header}}

	<aside>
		{{#prevPage}}<a href="{{url}}">previous</a>{{/prevPage}}
		{{#nextPage}}<a href="{{url}}">next</a>{{/nextPage}}
	</aside>

	{{# articles}}<article>{{> index}}</article>{{/ articles}}
	{{> footer}}
</body>
</html>
